<UserControl
    x:Class="iHentai.Apis.NHentai.Views.GalleryInfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:iHentai.Apis.NHentai.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:iHentai.Apis.NHentai.Models"
    DataContextChanged="GalleryInfoView_OnDataContextChanged"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:DataContext="{d:DesignInstance models:GalleryModel}">

    <Grid>
        <TextBlock x:Name="LanguageTextBlock" HorizontalAlignment="Left" />
        <TextBlock Text="{Binding UploadDate, Converter={StaticResource HumanizerConverter}}"
                   HorizontalAlignment="Right" />
    </Grid>
</UserControl>