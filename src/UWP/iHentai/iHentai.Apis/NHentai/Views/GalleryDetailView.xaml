<UserControl
    x:Class="iHentai.Apis.NHentai.Views.GalleryDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:iHentai.Apis.NHentai.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:iHentai.Apis.NHentai.Models"
    xmlns:converters="using:iHentai.Apis.NHentai.Converters"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:controls="using:iHentai.Apis.Core.Common.Controls"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance models:GalleryModel}"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <converters:TagConverter x:Key="TagConverter" />
        <converters:ThumbListConverter x:Key="ThumbListConverter" />
    </UserControl.Resources>

    <StackPanel>
        <controls:TagListView Margin="8" ItemsSource="{Binding Tags, Converter={StaticResource TagConverter}}" />
        <controls:ThumbListView ItemsSource="{Binding Converter={StaticResource ThumbListConverter}}" />
    </StackPanel>
</UserControl>