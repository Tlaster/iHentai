<UserControl
    x:Class="iHentai.Apis.NHentai.Views.GalleryDetailInfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:iHentai.Apis.NHentai.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:iHentai.Apis.NHentai.Models"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance models:GalleryModel}"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock
            TextWrapping="Wrap"
            IsTextScaleFactorEnabled="True"
            IsTextSelectionEnabled="True"
            Text="{Binding TitleModel.Japanese}" />
        <TextBlock Grid.Row="1" Text="{Binding UploadDate, Converter={StaticResource HumanizerConverter}}" />

    </Grid>

</UserControl>