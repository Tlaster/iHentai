<UserControl
    x:Class="iHentai.Apis.EHentai.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:iHentai.Apis.EHentai.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:iHentai.Apis.EHentai.Models"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:DataContext="{d:DesignInstance models:LoginData}">

    <StackPanel>
        <TextBox Header="User Name" Text="{Binding UserName,Mode=TwoWay}" AcceptsReturn="False"/>
        <PasswordBox Header="Password" Password="{Binding Password, Mode=TwoWay}"/>
    </StackPanel>
</UserControl>
