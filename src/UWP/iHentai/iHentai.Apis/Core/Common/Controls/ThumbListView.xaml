<controls:AdaptiveGridView
    x:Class="iHentai.Apis.Core.Common.Controls.ThumbListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:iHentai.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    ItemHeight="240" 
    DesiredWidth="160"
    SelectionMode="None" 
    IsItemClickEnabled="True"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <controls:AdaptiveGridView.ItemTemplate>
        <DataTemplate>
            <Grid Padding="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <controls:ImageEx IsCacheEnabled="True" Stretch="UniformToFill" Source="{Binding Link}"/>
                <TextBlock Grid.Row="1" Text="{Binding Page}" HorizontalAlignment="Center"/>
            </Grid>
        </DataTemplate>
    </controls:AdaptiveGridView.ItemTemplate>
</controls:AdaptiveGridView>

