<mvvm:MvvmPage
    x:Class="iHentai.Pages.ServiceSelectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:iHentai.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="using:iHentai.Mvvm"
    xmlns:viewModels="using:iHentai.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModels:ServiceSelectionViewModel}">

    <Grid>
        <ComboBox ItemsSource="{Binding Services}" SelectedItem="{Binding SelectedService, Mode=TwoWay}"/>
        <StackPanel Visibility="{Binding Api.HasLogin, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true}">
            <TextBox Header="User Name" Text="{Binding UserName, Mode=TwoWay}" AcceptsReturn="False"/>
            <PasswordBox Header="Password" Password="{Binding Password, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</mvvm:MvvmPage>