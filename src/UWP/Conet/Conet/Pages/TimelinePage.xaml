<mvvm:MvvmPage
    x:Class="Conet.Pages.TimelinePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Conet.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="using:iHentai.Mvvm"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:viewModels="using:Conet.ViewModels"
    xmlns:j="using:Conet.Apis.Core"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModels:TimelineViewModel}">

    <Grid>
        <controls:PullToRefreshListView ItemsSource="{Binding Source}">
            <controls:PullToRefreshListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock Text="{Binding text}" />
                    </Grid>
                </DataTemplate>
            </controls:PullToRefreshListView.ItemTemplate>
        </controls:PullToRefreshListView>
    </Grid>
</mvvm:MvvmPage>