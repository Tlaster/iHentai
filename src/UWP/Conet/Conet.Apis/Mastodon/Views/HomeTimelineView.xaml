<UserControl
    x:Class="Conet.Apis.Mastodon.Views.HomeTimelineView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Conet.Apis.Mastodon.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mastodon="using:Conet.Apis.Mastodon"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:services="using:iHentai.Services"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:DataContext="{d:DesignInstance mastodon:HomeTimelineViewModel}">

    <Grid>

        <controls:PullToRefreshListView ItemsSource="{Binding Source}" RefreshCommand="{Binding Source.RefreshCommand}">
            <controls:PullToRefreshListView.ItemTemplate>
                <DataTemplate>
                    <local:StatusView/>
                </DataTemplate>
            </controls:PullToRefreshListView.ItemTemplate>
        </controls:PullToRefreshListView>
    </Grid>
</UserControl>
