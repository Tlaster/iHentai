<UserControl
    x:Class="Conet.Apis.Mastodon.Views.StatusView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Conet.Apis.Mastodon.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:converters="using:Conet.Apis.Weibo.Views.Converters"
    xmlns:linq="using:Newtonsoft.Json.Linq"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance linq:JToken}"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <controls:ImageEx CornerRadius="25" Source="{Binding [account][avatar]}" Width="50" Height="50"/>
        <controls:MarkdownTextBlock  x:Name="WeiboContent" Grid.Row="1" Text="{Binding [content], Converter={StaticResource HtmlToMarkdownConverter}}" TextWrapping="WrapWholeWords" />
    </Grid>
</UserControl>
